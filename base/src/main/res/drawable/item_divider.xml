<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <size
        android:width="@dimen/height_divider"
        android:height="@dimen/height_divider"
        />
    <solid android:color="?attr/dividerColor" />
</shape>